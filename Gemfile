source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Keep this version in sync with .ruby-version and Dockerfile!
ruby '3.2.2'

gem 'rake'
gem 'sinatra'
gem 'thin'

group :test do
  gem 'minitest'
  gem 'rack-test'
end
